---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gpuunits.runtime.lokiwager.io
spec:
  group: runtime.lokiwager.io
  names:
    kind: GPUUnit
    listKind: GPUUnitList
    plural: gpuunits
    singular: gpuunit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.specName
      name: Spec
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.accessURL
      name: Access
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GPUUnit is the schema for one stock or active runtime unit.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GPUUnitSpec defines the desired state of GPUUnit.
            properties:
              access:
                description: Access describes the primary runtime endpoint.
                properties:
                  primaryPort:
                    type: string
                  scheme:
                    type: string
                type: object
              gpu:
                description: GPU is the number of GPUs requested by the unit workload.
                format: int32
                minimum: 0
                type: integer
              image:
                description: Image is the runtime image used by the unit workload.
                type: string
              memory:
                description: Memory is the memory request/limit for the unit workload.
                type: string
              specName:
                description: SpecName is the requested runtime flavor, for example
                  "g1.1".
                type: string
              template:
                description: Template is the runtime-facing pod slice owned by this
                  unit.
                properties:
                  args:
                    items:
                      type: string
                    type: array
                  command:
                    items:
                      type: string
                    type: array
                  envs:
                    items:
                      description: GPUUnitEnvVar describes one environment variable
                        injected into the runtime container.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  ports:
                    items:
                      description: GPUUnitPortSpec declares one named runtime port
                        and its transport protocol.
                      properties:
                        name:
                          type: string
                        port:
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          description: Protocol defines network protocols supported
                            for things like container ports.
                          type: string
                      required:
                      - name
                      - port
                      type: object
                    type: array
                type: object
            required:
            - specName
            type: object
          status:
            description: GPUUnitStatus defines the observed state of GPUUnit.
            properties:
              accessURL:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
              serviceName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
